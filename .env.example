# ============================================================================
# MCP TRADING SERVER CONFIGURATION
# ============================================================================
# This file contains all required environment variables for the OAuth-secured
# MCP trading server. Copy this to .env and fill in your values.

# ============================================================================
# REQUIRED: Security Configuration
# ============================================================================

# JWT Secret Key - Used for signing OAuth access tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your_jwt_secret_key_here

# Encryption Key - Used for encrypting user credentials in database
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_encryption_key_here

# ============================================================================
# REQUIRED: Server Configuration
# ============================================================================

# Server URL - Base URL where this server is accessible
# Examples:
#   - Production: https://your-app.railway.app
#   - Local dev: http://localhost:8000
SERVER_URL=http://localhost:8000

# Database URL - PostgreSQL connection string for production
# Examples:
#   - Railway: postgresql://user:pass@host:port/dbname
#   - Local dev: Leave commented to use SQLite (auto-generated)
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# ============================================================================
# OPTIONAL: Development Configuration
# ============================================================================

# Port for local development (default: 8000)
# PORT=8000

# ============================================================================
# DEPRECATED: Old Environment Variables (No Longer Used)
# ============================================================================
# The following variables were used in the old non-OAuth version:
# - TRADIER_SANDBOX_ACCESS_TOKEN
# - TRADIER_SANDBOX_ACCOUNT_NUMBER
# - TRADIER_ACCESS_TOKEN
# - TRADIER_ACCOUNT_NUMBER
# - TRADIER_SANDBOX
# - TRADIER_ENDPOINT
#
# In the OAuth version, credentials are stored per-user in the database
# and configured via the /setup endpoint.
